@inherits LayoutComponentBase

<div class="main-layout">
    @* <div class="sidebar">
        <NavMenu />
    </div> *@
    <div class="content">
        <div class="top-bar">
            <!-- Add top bar content like logo or user profile here -->
        </div>
        <div class="main-content">
            @Body
        </div>
    </div>
</div>

<style>
    .main-layout {
        display: flex;
        height: 100vh;
        width: 100vw;
    }

    .sidebar {
        width: 250px;
        background-color: #343a40;
        color: #fff;
        padding: 20px;
        box-shadow: 2px 0 5px rgba(0,0,0,0.1);
    }

    .content {
        flex: 1;
        display: flex;
        flex-direction: column;
    }

    .top-bar {
        height: 60px;
        background-color: #f8f9fa;
        border-bottom: 1px solid #ddd;
        padding: 10px 20px;
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .main-content {
        flex: 1;
        padding: 20px;
        background-color: #f5f5f5;
    }
</style>
